{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Debug Current TS File (tsx)",
            "request": "launch",
            "command": "tsx watch -- src/index.ts",
            "cwd": "${workspaceFolder}"
        }
    ]
}
